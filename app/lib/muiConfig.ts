import { createTheme } from "@mui/material";

export const muiConfig = createTheme({
  palette: {
    primary: {
      main: "#00D89D",
    },
  },
  typography: {
    fontFamily: "Chivo, sans-serif",
    fontSize: 13,
  },
});
